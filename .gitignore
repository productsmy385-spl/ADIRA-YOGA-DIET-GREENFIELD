# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files — real values never get committed, the annotated template always does
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Read-only database snapshots taken around a migration. Local evidence, not history —
# they carry timestamps and counts from one machine's view of one moment.
.baseline/

# Decoders copied from three by scripts/sync-3d-decoders.mjs at build time. They must
# match the installed three version, so they are generated rather than committed.
public/decoders/

# Local env backups taken when repointing the development database.
.env.local.bak-*
.env
